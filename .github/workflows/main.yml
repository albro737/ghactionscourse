name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
       - name: Print a Welcome message
         run: echoe "Welcome to our first Github Actions Workflow!"
  
  
